[phases.setup]
nixPkgs = ["python311", "pip", "gcc", "postgresql"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = []

[phases.start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT}"
